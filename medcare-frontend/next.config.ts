import type { NextConfig } from "next";

const nextConfig: NextConfig = {typescript: { ignoreBuildErrors: true }
  /* config options here */
};
"skip type errors for build" 

export default nextConfig;
